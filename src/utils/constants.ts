export const WIDTH = 800;
export const HEIGHT = 800;

export const CELL_SIZE = 20;

export const NUM_ROWS = WIDTH / CELL_SIZE;
export const NUM_COLS = HEIGHT / CELL_SIZE;

export const COLORS = ["black", "green"];

export const IS_DEAD = 0;
export const IS_ALIVE = 1;
